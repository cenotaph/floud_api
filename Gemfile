source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in floud_api.gemspec
gemspec
gem 'dotenv-rails', groups: %i[development test]
